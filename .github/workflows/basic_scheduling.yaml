name: "Basic: Scheduling"

on:
    schedule:
        - cron: "*/2 * * * *"

jobs:
    foo_job:
        runs-on: ubuntu-latest
        name: foo_job
        steps:
            - run: |
                  echo "foo"
